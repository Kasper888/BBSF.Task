"use strict"; var KTWizard2 = function () {
    var e, r, i; return {
        init: function () {
            var t;
            KTUtil.get("kt_wizard_v2"),
                e = $("#kt_form"),
                (i = new KTWizard("kt_wizard_v2", { startStep: 1 })).on("beforeNext",
                    function (e) {
                        if (e.currentStep == 1) {
                            firstNextClick(e)
                            
                        }
                        if (e.currentStep == 2 && !secondNextClick()) {
                            e.stop();

                        }
                    }),
                i.on("change", function (e) { KTUtil.scrollTop() }),
                r = e.validate()
        }
    }
}(); jQuery(document).ready(function () { KTWizard2.init() });